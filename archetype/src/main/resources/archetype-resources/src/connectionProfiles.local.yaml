connectionProfiles:
  profiles:
    test_h2:
      connectionUrl: jdbc:h2:~/${artifactId};AUTO_SERVER=TRUE
      username: sa
      password: ''
    test_local:
      connectionUrl: jdbc:postgresql://localhost:5432/${artifactId}
      username: test
      password: test
    test_database_docker:
      connectionUrl: jdbc:postgresql://localhost:54321/${artifactId}
      username: postgres
      password: postgres
    uuinnk_local:
      connectionUrl: jdbc:postgresql://localhost:5432/${artifactId}
      username: postgres
      password: pg
